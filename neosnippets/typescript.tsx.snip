include ./javascript.jsx.snip

snippet for
abbr for loop snippet
options indent
  const ${1:len} = ${2:list}.length;
  for (let ${3:i} = 0; $3 < $1; $3 += 1) {
    const ${4:item} = $2[$3];
    ${5}
  }

snippet if
abbr if block
options indent
  if (${1:condition}) {
    ${2}
  }

snippet ife
abbr if else block
options indent
  if (${1:condition}) {
    ${2}
  } else {
    ${3}
  }
